---
title: 189. Rotate Array
date: 2025-10-18
category: 'leetcode'
---

[189. Rotate Array](https://leetcode.cn/problems/rotate-array/)

![Screenshot 2025-10-18 at 10.43.03 pm](assets/Screenshot%202025-10-18%20at%2010.43.03%E2%80%AFpm.png)

```js
/**
 * @param {number[]} nums
 * @param {number} k
 * @return {void} Do not return anything, modify nums in-place instead.
 */
var rotate = function (nums, k) {
  k = k % nums.length
  const tmp = k ? nums.slice(-k) : []
  console.log(tmp)
  nums.splice(-k, k)
  nums.splice(0, 0, ...tmp)
}
```

一句话思路：原地反转所有元素，在反转前k个元素，在反转后n-k个元素

![lc189.png](https://pic.leetcode.cn/1716207476-TyuIlk-lc189.png)

更好的答案

```js
/**
 * @param {number[]} nums
 * @param {number} k
 * @return {void} Do not return anything, modify nums in-place instead.
 */
var rotate = function (nums, k) {
  k %= nums.length
  // 反转 [i, j] 的所有元素
  function reverse(i, j) {
    while (i < j) {
      ;[nums[i], nums[j]] = [nums[j], nums[i]]
      i++
      j--
    }
  }
  reverse(0, nums.length - 1)
  reverse(0, k - 1)
  reverse(k, nums.length - 1)
}
```
